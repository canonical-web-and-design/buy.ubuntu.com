{% if template contains 'index' %}	
  <section style="background-color: {{ settings.homepage_hero_bgcolor }}; color: {{ settings.homepage_hero_textcolor }};">
    <div class="p-strip is-deep" style="background-image: url({{ 'homepage_hero-graph.png' | asset_url }});">
      <div class="u-fixed-width">
        <h1>{{ settings.homepage_main_heading }}</h1>
        <h2 class="p-heading--four">{{ settings.homepage_sub_heading }}</h4>
        <a class="p-button--positive" href="/collections/ubuntu-advantage-for-infrastructure-essential">Get started with our Essential plan from $25</a>
        <p><a class="p-link--inverted p-link--external" href="https://ubuntu.com/support" title="Get UA Infrastructure Essential" target="blank">Learn more about our support options</a></p>
      </div>
    </div>
  </section>
{% elsif template contains 'cart' %}
  <header class="cart-header" style="background-color: #f1f1f1;">
    {% include 'site-nav' %}

    <div class="p-strip--light">
      <div class="row">
        <div class="col-12">
          <h1>Shopping Cart</h1>
          {% if cart.item_count < 1 %}
            <h2>It appears that your cart is currently empty!</h2>
            <h3>You can continue browsing <a href="/">here</a>.</h3>
          {% endif %}
        </div>
      </div>
    </div>
  </header>

{% elsif template contains 'collection' %}
  <div class="u-hide">{{ collection.handle }}</div>

  <section style="background-color: {{ bgcolor }}; color: {{ txcolor }};">
    <div class="p-strip">
      <div class="row u-vertically-center">
        <div class="col-8">
          <h1>{{ collection.title }}</h1>
          
          {% unless collection.description == blank or collection.description.empty? %}
            <p class="p-heading--four">{{ collection.description }}</p>
          {% endunless %}
        </div>

        {% unless collection.image == blank or collection.image.empty? %}
          <div class="col-start-large-10 col-3 u-hide--small">
            <img src="{{ collection.image | img_url: "master" }}" alt="{{ collection.title }}" />
          </div>
        {% endunless %}
      </div>
    </div>
  </section>
{% else %}
  <section>
    <div class="p-strip">
      <div class="u-fixed-width">
        <h1>{{ template }}</h1>
      </div>
    </div>
  </section>
{% endif %}
